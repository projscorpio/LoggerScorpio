name: BUILD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: installdep
      run: ./scripts/ci_env_setup.sh 
      run: ./scripts/installdep.sh
    - name: build
      run: ./scripts/ci_env_setup.sh 
      run: ./scripts/buildall.sh
